# Create a Kubernetes Service for Nginx
---
apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: wordpress
spec:
  selector:
    app: wordpress
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80